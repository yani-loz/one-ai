# STAGE-A sealed oracle — amendment log

`STAGE-A-SEAL.sha256` is the byte seal over the files of the Stage A sealed oracle (50 at closure, 85 after amendment 14)
(`backend/tests/tools/`). Verify from `backend/` with `sha256sum -c ../docs/PM/memory/phase0/STAGE-A-SEAL.sha256`.
The seal was closed 2026-09-06 ~20:15 (Europe/Sofia). Every change after closure is an
amendment: authored ONLY by the oracle author, approved by the orchestrator, proven
byte-exact by reverse hashing (undo the described change → the previous digest returns),
and never touched by an implementer.

| # | When (Sofia) | Files | Change | Previous digest → new digest | Proof |
|---|---|---|---|---|---|
| 1 | 2026-09-06 ~22:10 | `seeding.py`, `seeding_big.py`, `seeding_rows.py` | Parents-first staged flushes in the probe seeding helpers. SQLAlchemy's unit of work orders mappers by module/class name when no `relationship()` exists, so `AclGrant` flushed before `Person` and violated the FK. Flush staging only; no seeded value, count, id or text changed. | `seeding.py` `7b5e7d69…` → `23e295c0…` · `seeding_big.py` `9f1f1042…` → `8a802e07…` · `seeding_rows.py` `6193bc5b…` → `f583e926…` | Author reverse-hash: reverting the staging restores all three closed-seal digests. |
| 2 | 2026-09-06 ~22:48 | `seeding.py` line 79 | E1 body text `… one … first` → `… one … „first“` (six bytes). `test_snapshot.py:284` asserts one seeded text carries both U+2026 and U+201E so a normalizing emitter fails; no seeded text ever did. The assertion had never executed under the closed seal (the fixture failed earlier on the missing package), so this is a latent oracle defect fixed in the DATA, not by weakening the assertion. | `seeding.py` `23e295c0…` → `69d354c1…` | Orchestrator reverse-hash: replacing `„first“` with `first` in the current file yields `23e295c0…` exactly. |
| 3 | 2026-09-07 ~01:05 | 11 NEW files: `test_probe_child_env.py`, `test_observer_scope.py`, `test_result_block_aborted_projection.py`, `test_verify_step1_frozen_expect_lock.py`, `test_hidden_budget_display.py`, `test_roster_hidden_subset.py`, `test_validation_guard_seal.py`, `test_runner_output_relpath.py`, `test_app_logging_capture.py`, `test_gate_context_errors.py`, `test_probe_db_staleness.py` | Sealing tests for fix-registry group A (rows A1–A12), written BEFORE the fixes; reviewed cross-vendor by Codex (NOT SOUND → 7 confirmed/qualified fixes applied, table in `STAGE-A-FIX-REGISTRY.md`). 57 tests: 53 fail on exactly the surface their registry row names, 4 pass by design as positive controls. The 50 existing files are byte-identical (no A3 amendment was needed: the sealed frozen invocations already pass `--expect-lock`, `scenario_fixtures.py:42`). Seal grows from 50 to 61 lines. | 50 unchanged; 11 added (hashes in the .sha256) | Author final run 00:45–01:03Z, fleet stopped: 601 collected, 547 passed, 54 failed (53 new-surface + 1 known A12 concurrency flake), 0 errors; orchestrator re-verified all 61 hashes and `ruff format --check` (61 formatted). |
| 4 | 2026-09-07 ~07:40 | 1 NEW file: `test_review_round_a.py` (12 tests) | Sealing tests for fix-registry round 2 (rows A2-b, A13, A14; contract §16.16 (p)–(r)): `split_evaluated` required on aborted hidden stdout blocks, roster-based H-gate reduction in projector and validator, the `__pycache__` suffix rule for the observer and the closure. 8 tests fail on exactly their missing behaviour, 4 pass by design as positive controls. The first draft of the (r) brief wrongly made an in-scope file an offender; the author caught the conflict and the test was corrected before hashing (superseded hash bbf72b0b…). Seal grows from 61 to 62 lines. | 61 unchanged; 1 added (`5b95ec37…`) | Orchestrator re-verified the hash on disk, 61 OK, `ruff format --check` 62 formatted, no unlisted file. |
| 5 | 2026-09-07 ~07:40 | `test_result_block_aborted_projection.py` `_stdout_shape` (amendment-3 file): one comment line + the `hidden_gates` construct (one hunk) | The hand-made stdout shape derived hidden-evidence gates from the criteria entries on the evaluated split; §16.16(q) (amendment 4) requires every `H_SPLIT_GATES` gate to reduce to `{status}` and the validator enforces it, so the older positive control contradicted the newer seal (5 tests). The helper now unions the hand-spelled roster; strictly less reaches stdout. | `9e7bbc8d…` → `bce01832…` | Author reverse-hash; orchestrator verified the saved pre-amendment copy hashes to `9e7bbc8d…` and `diff -u` against the current file is exactly that one hunk (the file is CRLF, which defeated a naive LF reconstruction). |
| 6 | 2026-09-07 ~08:15 | `test_verify_step1_cli.py`, `test_verify_step1_frozen.py`, `test_verify_step1_frozen_expect_lock.py` (two hunks each: a shared `_assert_aborted_gates` helper inserted, the four-line `assert all(...)` replaced) | The three original assertions required `reason == "aborted"` on EVERY gate of an aborted block printed on stdout; §16.16(q) (amendment 4) makes the four H-split gates of a HIDDEN run print `{"status": "skipped"}` with no reason (5 sealed tests, `KeyError: 'reason'`). The helper is run-kind aware: every gate skipped; on checkpoint/validation the H gates are exactly `{status}` and the others keep `reason == "aborted"`; on tuning every gate keeps it (A2 makes the tuning projection the identity). | `a26054b3…` → `bec24fa4…`, `1c0cfd6b…` → `b74693fc…`, `55e42494…` → `1d66b142…` | Author reverse-hash on raw CRLF bytes; orchestrator verified the three saved pre-amendment copies hash to the sealed digests and `diff -u` per file shows only the two described hunks. |
| 7 | 2026-09-07 ~08:50 | `test_review_round_a.py` line 232 (one line) | The non-H control asserted `{status, reason, criteria}` on the projected CH gate, but the test's own aborted builder gives non-QS/SNAP gates `{status, reason}` (no criteria) — unsatisfiable on the two aborted params without fabricating ids (found by the round-2 fixer, verified by the orchestrator). The control is now shape-agnostic: a non-H gate passes through unchanged. | `5b95ec37…` → `46a0b29f…` | Author reverse-hash on raw CRLF bytes; orchestrator verified the saved copy hashes to the sealed digest and `diff -u` is exactly that one line. |
| 8 | 2026-09-07 ~10:30 | 2 NEW files: `test_write_guarded_degrades_any_exception.py` (A22), `test_merkle_pycache_rule.py` (A24) | Sealing tests for round 3: the artifact-write guard degrades ANY exception class to its name (R5/R11), and `merkle_sha256`'s defaults follow §16.16(r) — only `*.pyc`/`*.pyo` excluded, a non-bytecode file under `__pycache__` moves the digest (the gap the mutation check found as M17). 8 tests: 6 fail on their named behaviour, 2 pass as controls. `reference.merkle_sha256_reference` (sealed harness) deliberately keeps the directory exclusion as an alarm for a smuggled file in the runner folder. Seal 62 → 64. | 62 unchanged; 2 added (`531c1e6c…`, `02df3132…`) | Orchestrator verified both hashes on disk, no unlisted file, `ruff format --check` 64 formatted. |
| 9 | 2026-09-07 ~11:55 | 2 NEW files: `test_entry_guard.py` (A25), `test_bytecode_suffix_chain.py` (A26) | Sealing tests for round 4: the single R11 guard at the entry point (an invalid `POSTGRES_PORT` or `APP_ENV=production` must yield exit 2 and `MEM01 INTERNAL ERROR: <class>` with no traceback, in BOTH invocation forms — today: exit 1 + traceback, and a non-UTF-8 stderr on the production path), and the suffix-chain bytecode rule (`hashing.is_bytecode`; CPython temp names `x.cpython-312.pyc.<id>` excluded from merkle, the closure and `is_manifested`; `__pycache__/oracle.json` kept). 12 tests, all fail today for the sealed reasons. Seal 64 → 66. | 64 unchanged; 2 added (`f6c8df03…`, `1cf54912…`) | Orchestrator verified both hashes on disk, no unlisted file, `ruff format --check` 66 formatted. |
| 10 | 2026-09-07 ~19:40 | 12 NEW files: the helper `review_round_5_harness.py` (not collected) and `test_review_round_5_red.py`, `test_review_round_5_red_b.py`, `test_review_round_5_cov.py`, `test_review_round_5_logging.py`, `test_review_round_5_logging_b.py`, `test_review_round_5_runner_a.py`, `test_review_round_5_runner_b.py`, `test_review_round_5_runner_c.py`, `test_review_round_5_hidden_budget.py`, `test_review_round_5_pure.py`, `test_review_round_5_pure_b.py`; ONE amended file: `test_hidden_budget.py` line 172 (`HiddenBudget(ledger)` → `HiddenBudget(ledger, results_root=tmp_path / "hidden")`) | Sealing tests for round 5 (fix-registry rows A28–A40; contract v1.2.9 §16.17 (a)–(l)) after the Codex cross-vendor review of the instrument code, after the Codex review of the tests and the author's fix round: 44 tests fail today on their ruled surfaces (43 new + the amended one), 8 are positive controls / negative pins (author report: session scratchpad `oracle_round5_report.md`; both Codex passes reconciled in the registry). The amendment moves the protected-result cache under a `results_root` (§16.17(h)); the amended test is red today on the unknown keyword. | `test_hidden_budget.py` `0a598771…` → `5f5a038e…`; new: `review_round_5_harness.py` `6fd1a16b…`; `test_review_round_5_red.py` `a220b61f…`; `test_review_round_5_red_b.py` `38d5c965…`; `test_review_round_5_cov.py` `8c9c7d7c…`; `test_review_round_5_logging.py` `133cce5c…`; `test_review_round_5_logging_b.py` `25440e3f…`; `test_review_round_5_runner_a.py` `f46c7c00…`; `test_review_round_5_runner_b.py` `ef2041b6…`; `test_review_round_5_runner_c.py` `9ff39cc8…`; `test_review_round_5_hidden_budget.py` `56b61965…`; `test_review_round_5_pure.py` `0a9c7205…`; `test_review_round_5_pure_b.py` `8010637c…` | The author's byte-for-byte pre-amendment copy (`test_hidden_budget.sealed.py`, CRLF) hashes to the sealed line `0a59877122ad…` (verified by the orchestrator with `sha256sum`); `diff -u` = the one line above. |
| 11 | 2026-09-07 ~21:48 | 1 NEW file(s): `test_review_round_6_red.py` | Round-6 sealing test for fix-registry row A41 (contract §16.17(b) as amended: the §16.9 fragment scan runs over the WHOLE `logging` output regardless of length — the post-fleet lens found the case-text region rule leaving fragments unscanned on a log longer than the whole-scan limit). The seal is red today on that defect; its three controls (positional rule on a text-carrying surface, short log, whole secret) are green. | new: `test_review_round_6_red.py` `0974c5a2…` | No existing sealed file changed. |
| 12 | 2026-09-07 ~22:21 | 3 NEW file(s): `test_review_round_7_runner.py`, `test_review_round_7_runner_b.py`, `test_review_round_7_pure.py` | Round-7 sealing tests for fix-registry rows A42–A45 after the Codex diff-scoped review of the round-5 code (contract §16.17 (d), (e), (j) as amended): a capture-close failure is classified and folded like an emit failure and `close_app_log` never raises; the free repeat reads emit failures before closing; `main` catches `CancelledError`; the covering reset and the proposed run must both carry the aborted admission's own candidate. Red today on those defects; controls green. | new: `test_review_round_7_runner.py` `697448fb…`; `test_review_round_7_runner_b.py` `d309e109…`; `test_review_round_7_pure.py` `70f4d999…` | No existing sealed file changed. |
| 13 | 2026-09-08 ~09:26 | `test_census.py` | Round 8 (contract v1.2.10 §16.18): `test_take_census_is_deterministic_over_an_unchanged_org` compares `(value, sql)` for every `CENSUS_V1` key EXCEPT `schema_database_size_bytes`, and asserts that informational key's presence, identical `sql` and positive integer value in both censuses; the `corpus_digest` equality is unchanged. Cause: the uninterrupted clean-gate run of 2026-09-08 (707/708) saw `pg_database_size` move 11426839 → 11272719 between two consecutive censuses on a freshly seeded probe under autovacuum — an on-disk measure outside MVCC that cannot be deterministic by construction. | `test_census.py` `664fe6f4…` → `de99da5c…` | The orchestrator's byte-for-byte pre-amendment copy (`test_census.sealed.py`, session scratchpad) hashes to the sealed line `664fe6f4d74f…` (verified with sha256 before the author edited); `diff -u` = 12 changed line(s) inside that one test function only. |
| 14 | 2026-09-08 ~09:26 | 3 NEW file(s): `test_review_round_8_guard.py`, `test_review_round_8_guard_b.py`, `test_review_round_8_hidden_budget.py` | Round-8 sealing tests for fix-registry rows A47 and A48 after the Codex review of the round-6/7 code (contract v1.2.10 §16.18 (b), (c)): an attempt id carrying more than one `validation_admission` on the lock refuses naming the attempt id only (same or different candidates) and `foreign` considers every admission of a verdict-bearing attempt; the free repeat replays the result whose deciding outcome appears LAST in the ledger (completion order) and never opens the superseded cache. Red today on those defects; controls green. | new: `test_review_round_8_guard.py` `f67a07ef…`; `test_review_round_8_guard_b.py` `aed127d6…`; `test_review_round_8_hidden_budget.py` `7efe6c49…` | No existing sealed file changed. |
| 15 | 2026-09-08 ~10:23 | ALL 48 sealed files (and the 73 instrument/tool files beside them) | Line-ending normalization CRLF → LF, no other byte changed: the sealed files had been written with CRLF while the repository's `.gitattributes` declares `*.py text eol=lf` (and `core.autocrlf=true` here), so any fresh checkout would restore LF bytes and fail the byte seal; `.gitattributes` now also pins `backend/tools/**` and `backend/tests/tools/**` to LF so the seal and `runner_sha256` are identical on every platform. Not a test change: ruff, the import check and the whole sealed suite were re-run alone after it (registry, "Final gate after round 8, LF"). | every sealed line replaced; old → new per file in the seal's git history | For every sealed file, replacing each `\n` of the new bytes by `\r\n` reproduces the old sealed digest (verified 48/48 by the orchestrator's script before writing); no file contained a lone `\r`; binaries (NUL-bearing) untouched: 0. |

Not amendments (format-only, outside the seal): the TIME battery ids under
`backend/tools/mem01_verify/fixtures/` were renamed `time_cases-NNN` → `time-NNN` (§16.8).
