/**
 * Role: Public surface of the Connect feature — the route-level page. Internal modules
 *       (client, hook, drawer, badge, providers) are not re-exported.
 * Used by: App.tsx (the /connectors route).
 */
export { ConnectorsPage } from "./ConnectorsPage";
