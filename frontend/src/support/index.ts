/**
 * Role: Public surface of the break-glass support module — the platform request panel and
 *       the company HITL approval inbox.
 * Used by: platform/OrganizationDetailPage (panel), admin/AdminConsolePage (inbox).
 */
export { SupportAccessPanel } from "./SupportAccessPanel";
export { SupportInbox } from "./SupportInbox";
